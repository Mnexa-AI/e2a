# Reviewed lifecycle reclassification for the gate/scan review resource. The
# four review operations plus the flag verdict fields were emitted stable in
# v1.0.10; hold_reason and the detail protection evidence then landed through
# PRs #389–#390. Product has explicitly classified this boundary as beta. Keep
# every rule fully qualified: the gate must continue rejecting all other
# stability downgrades.
POST /v1/reviews/{id}/approve endpoint stability level decreased from `stable` to `beta`
GET /v1/reviews/{id} endpoint stability level decreased from `stable` to `beta`
GET /v1/reviews endpoint stability level decreased from `stable` to `beta`
POST /v1/reviews/{id}/reject endpoint stability level decreased from `stable` to `beta`
GET /v1/agents/{email}/messages/{id} response property `hold_reason` stability level decreased from `stable` to `beta`
GET /v1/agents/{email}/messages/{id} response property `protection` stability level decreased from `stable` to `beta`
GET /v1/agents/{email}/messages/{id} response property `protection/items/` stability level decreased from `stable` to `beta`
GET /v1/agents/{email}/messages/{id} response property `protection/items/categories/items/` stability level decreased from `stable` to `beta`
POST /v1/agents/{email}/messages/{id}/restore response property `hold_reason` stability level decreased from `stable` to `beta`
POST /v1/agents/{email}/messages/{id}/restore response property `protection` stability level decreased from `stable` to `beta`
POST /v1/agents/{email}/messages/{id}/restore response property `protection/items/` stability level decreased from `stable` to `beta`
POST /v1/agents/{email}/messages/{id}/restore response property `protection/items/categories/items/` stability level decreased from `stable` to `beta`
GET /v1/reviews response property `items/items/hold_reason` stability level decreased from `stable` to `beta`
GET /v1/reviews/{id} response property `hold_reason` stability level decreased from `stable` to `beta`
GET /v1/reviews/{id} response property `protection` stability level decreased from `stable` to `beta`
GET /v1/reviews/{id} response property `protection/items/` stability level decreased from `stable` to `beta`
GET /v1/reviews/{id} response property `protection/items/categories/items/` stability level decreased from `stable` to `beta`
GET /v1/agents/{email}/conversations/{id} response property `messages/items/flag_reason` stability level decreased from `stable` to `beta`
GET /v1/agents/{email}/conversations/{id} response property `messages/items/flagged` stability level decreased from `stable` to `beta`
GET /v1/agents/{email}/messages response property `items/items/flag_reason` stability level decreased from `stable` to `beta`
GET /v1/agents/{email}/messages response property `items/items/flagged` stability level decreased from `stable` to `beta`
GET /v1/agents/{email}/messages/{id} response property `flag_reason` stability level decreased from `stable` to `beta`
GET /v1/agents/{email}/messages/{id} response property `flagged` stability level decreased from `stable` to `beta`
POST /v1/agents/{email}/messages/{id}/restore response property `flag_reason` stability level decreased from `stable` to `beta`
POST /v1/agents/{email}/messages/{id}/restore response property `flagged` stability level decreased from `stable` to `beta`

# Reviewed pre-GA authentication-contract correction. `from` mixed Header-From
# and Reply-To semantics; `header_from` now names the literal identity. This
# exception is deliberately limited to the four exact response projections
# that removed the misleading required field before the announced GA tag.
GET /v1/agents/{email}/conversations/{id} removed the required property `messages/items/from` from the response with the `200` status
GET /v1/agents/{email}/messages removed the required property `items/items/from` from the response with the `200` status
GET /v1/agents/{email}/messages/{id} removed the required property `from` from the response with the `200` status
POST /v1/agents/{email}/messages/{id}/restore removed the required property `from` from the response with the `200` status
